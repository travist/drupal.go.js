{
  "name": "drupal.go.js",
  "version": "0.0.1",
  "description": "A node.js package to automate and test Drupal using Zombie.js.",
  "main": "./lib/drupal.js",
  "engines": { "node": ">= 0.2.0" },
  "dependencies": {
    "zombie": ">=0",
    "assert": ">=0",
    "nconf": ">=0",
    "prompt": ">=0",
    "underscore": ">=0",
    "async": ">=0"
  }
}